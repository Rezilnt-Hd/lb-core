/**
 * GENERATED by lb-keyword-engine/scripts/generate-geo-neighbors.ts — do not hand-edit.
 * Metro -> nearby-area map for the geographic keyword overflow tier, sourced from
 * the public-domain US Census Gazetteer Places file. Keyed by normalizeCity(city,
 * state) = "<city>|<state>". Values are within-radius areas, distance-ordered and
 * capped at MAX_GEO_RUNGS. An ABSENT key yields no geo rungs (fail-safe). Static +
 * pure so buildLadder stays deterministic and synchronous.
 */
export declare const MAX_GEO_RUNGS = 25;
export declare const GEO_NEIGHBORS: Record<string, string[]>;
/** Nearby curated areas for a normalized city key, capped at MAX_GEO_RUNGS.
 * Returns [] for an uncurated city. */
export declare function geoNeighbors(cityKey: string): string[];
